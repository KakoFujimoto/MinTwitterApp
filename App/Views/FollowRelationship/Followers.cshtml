@model List<MinTwitterApp.DTO.FollowRelationshipDTO>

@{
    ViewData["Title"] = "フォロワー一覧";
}

<h2>フォロワー一覧</h2>

@if (Model.Count == 0)
{
    <p>フォロワーはいません。</p>
}
else
{
    <ul class="list-group">
        @foreach (var user in Model)
        {
            <li class="list-group-item">
                <a asp-controller="User" asp-action="Profile" asp-route-id="@user.UserId">
                    @user.Name
                </a>
            </li>
        }
    </ul>
}
